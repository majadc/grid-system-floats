{
  "name": "grid_system-floats",
  "version": "1.0.0",
  "description": "grid system based on floats",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:sass": "sass --watch sass/style.scss:css/style.css",

    "compile:sass": "sass sass/style.scss css/style.comp.css",
    "prefix:css": "postcss css/style.comp.css --use autoprefixer -o css/style.prefix.css",
    "build:css": "npm-run-all compile:sass prefix:css"


  },
  "author": "majadc",
  "license": "ISC"
}
